{% extends '/layout_manager.html.twig' %}

{% block page_title %}
    myCLAP Manager - Ajouter une vidéo
{% endblock %}

{% block content %}

    <div class="header-center no-border">
        <div>
            <h2>Vidéos</h2>
            <h1>Ajouter une vidéo</h1>
            <p class="max-w-1000 ms-auto">
                A partir de ce formulaire vous allez pouvoir précisez les détails de la vidéo que vous souhaitez ajouter
                au site, après cela vous serez invité à sélectionner le fichier à mettre en ligne.
            </p>
            <div class="mb-25">
                <div class="alert alert-info">
                    <p>
                        Un petit guide est disponible pour vous guider dans la mise en ligne d'une vidéo sur myCLAP : <a href="/MettreEnLigne.zip">Télécharger le guide</a>.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <div class="overlay p-20 max-w-1000 ms-auto br-5">
        <form method="{{ form.method }}" action="{{ form.action }}" enctype="{{ form.enctype }}" class="mb-0">

            {{ form.csrf_token|render }}

            {% if form.errors|length > 0 %}
                <div class="p-10">
                    <div class="alert alert-error">
                        {% if form.errors|length > 1 %}
                            <ul>
                                {% for error in form.errors %}
                                    <li>{{ error.message }}</li>
                                {% endfor %}
                            </ul>
                        {% else %}
                            <p>{{ form.errors.get(0).message }}</p>
                        {% endif %}
                    </div>
                </div>
            {% endif %}

            <div class="field">
                {{ form.name|render }}
            </div>
            <div class="field">
                {{ form.created_on|render }}
            </div>
            <div class="mb-10">
                {{ form.thumbnail_identifier|render }}
                <div class="field">
                    {{ form.thumbnail_file|render }}
                </div>
            </div>
            <div class="field">
                {{ form.description|render }}
            </div>
            <div class="field">
                {{ form.categories|render }}
            </div>
            <div class="field">
                {{ form.access|render }}
            </div>

            <hr class="mt-25 mb-15">

            <div class="text-right">
                <button class="button red" type="submit">Suivant</button>
            </div>
        </form>
    </div>
{% endblock %}